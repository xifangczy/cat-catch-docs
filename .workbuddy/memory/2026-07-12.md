# 2026-07-12

## cat-catch-docs 文档维护
- 新增章节 `src/docs/aria2.md`（Aria2 RPC 远程下载，配合 Motrix Next）。
- 校对修正：① 缺动词「是一款」；② 错别字「开起」→「开启」、「PRC」→「RPC」；③「命令方式」→「命令行方式」、「内置有」→「内置了」；④ 多处缺逗号/介词；⑤ `[popup](popup)` 改为 `/docs/popup`。
- 翻译英文版到 `src/en/docs/aria2.md`。
- 在 `.vitepress/config.mts` 的中英文侧边栏「工具 & 集成 / Tools & Integration」末尾（媒体控制之后）加入 Aria2 RPC 链接。
- 约定：文档图片放 `src/public/assets/`，引用用 `/assets/xxx.png`；站点内部文档互链用绝对路径 `/docs/xxx`（英文 `/en/docs/xxx`）。
